import { AppScreen } from '../../src/components/app-screen'

export default function WishlistScreen() {
  return <AppScreen title="Deseos" description="Vista de lista o tarjetas con foto, prioridad, ahorro actual y fecha estimada de compra." />
}
