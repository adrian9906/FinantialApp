import { AppScreen } from '../../src/components/app-screen'

export default function ExpensesScreen() {
  return <AppScreen title="Gastos" description="CRUD de gastos, categorías esenciales y cálculo efectivo compartido." />
}
