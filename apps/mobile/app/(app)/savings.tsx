import { AppScreen } from '../../src/components/app-screen'

export default function SavingsScreen() {
  return <AppScreen title="Ahorros" description="Entradas de ahorro, overview mensual y rollover configurado desde settings." />
}
