import { AppScreen } from '../../src/components/app-screen'

export default function EventsScreen() {
  return <AppScreen title="Eventos" description="Calendario y lista con tope presupuestario ligado a gustos y saldo disponible." />
}
